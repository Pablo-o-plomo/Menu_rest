'use client';
import { useEffect, useState } from 'react';
const keys=['category','itemName','costPrice','salePrice','effectiveDate'];
export default function PricesUpload(){const [restaurants,setR]=useState<any[]>([]);const [rid,setRid]=useState('');const [mapping,setM]=useState<any>({category:'category',itemName:'itemName',costPrice:'costPrice',salePrice:'salePrice',effectiveDate:'effectiveDate'});const [preview,setP]=useState<any>(null);const [snapshotDate,setD]=useState('');const [comment,setC]=useState('');useEffect(()=>{fetch('/api/restaurants').then(r=>r.json()).then(setR)},[]);
async function prev(e:any){e.preventDefault();const fd=new FormData();fd.append('file',e.target.file.files[0]);fd.append('mapping',JSON.stringify(mapping));const r=await fetch('/api/uploads/prices/preview',{method:'POST',body:fd});setP(await r.json());}
async function confirm(){await fetch('/api/uploads/prices/confirm',{method:'POST',body:JSON.stringify({...preview,restaurantId:rid,snapshotDate,comment})});alert('Импорт цен завершен');}
return <div><h1>Загрузка цен</h1><div className='card'><div className='toolbar'><select className='select' value={rid} onChange={e=>setRid(e.target.value)}><option value=''>Ресторан</option>{restaurants.map(r=><option key={r.id} value={r.id}>{r.name}</option>)}</select>{keys.map(k=><input key={k} className='input' value={mapping[k]} onChange={e=>setM({...mapping,[k]:e.target.value})} placeholder={k}/>)}<input className='input' type='date' value={snapshotDate} onChange={e=>setD(e.target.value)}/><input className='input' placeholder='Комментарий' value={comment} onChange={e=>setC(e.target.value)}/></div><form onSubmit={prev}><input className='input' type='file' name='file' accept='.xlsx,.xls,.csv'/><button className='btn'>Предпросмотр</button></form></div>{preview&&<div className='card'><h3>Ошибки</h3>{preview.errors?.map((e:string,i:number)=><div key={i} className='bad'>{e}</div>)}<pre>{JSON.stringify(preview.rows?.slice(0,10),null,2)}</pre><button className='btn' disabled={preview.critical} onClick={confirm}>Подтвердить импорт</button></div>}</div>}
