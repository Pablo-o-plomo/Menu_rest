import { NextResponse } from 'next/server';import { parseUpload } from '@/lib/parse';import { mapMenuImportRow } from '@/lib/import-mapping';
export async function POST(req: Request){const fd=await req.formData();const file=fd.get('file') as File;const restaurantId=Number(fd.get('restaurantId'));const rows=(await parseUpload(file)).map(mapMenuImportRow);return NextResponse.json({restaurantId,fileName:file.name,rows,invalidRows:rows.filter(r=>!r.name||r.salePrice<=0).length});}
